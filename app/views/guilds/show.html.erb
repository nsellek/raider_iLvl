<%= button_to "Back", dashboard_path, class: "btn btn-primary", method: :get %>
<div class="container">
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <table class="table">
        <tr>
          <th></th>
          <th>Name</th>
          <th>Class</th>
          <th>Realm</th>
          <th>Level</th>
        </tr>
        <% @members.each do |member| %>
        <tr>
          <td><img src="<%= member['thumbnail'] %>" alt=""></td>
          <td><%= member['name'] %></td>
          <td><%= member['class_name'] %></td>
          <td><%= member["realm"] %></td>
          <td><%= member["level"] %></td>
        </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>
